{
  "name": "system-monitor-extension",
  "version": "0.1.0",
  "description": "Lightweight Manifest V3 Chrome extension popup for system metrics.",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24.15.0",
    "pnpm": ">=11.9.0"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "pnpm clean && tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "test": "pnpm build && node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
